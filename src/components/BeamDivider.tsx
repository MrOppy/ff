export default function BeamDivider() {
    return (
        <div className="beam-divider" aria-hidden="true">
            <span className="beam-divider__beam beam-divider__beam--a" />
            <span className="beam-divider__beam beam-divider__beam--b" />
        </div>
    );
}
